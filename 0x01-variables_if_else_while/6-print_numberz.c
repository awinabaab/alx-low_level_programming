#include <stdio.h>

/**
 * main - Print all single digit numbers of base 10 from 0 with putchar
 *
 * Return: (0)
 */

int main(void)
{
	for (int num = 48; num <= 57; num++)
		putchar(num);
	putchar('\n');
	return (0);
}
