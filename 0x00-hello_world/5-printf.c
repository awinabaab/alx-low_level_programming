#include <stdio.h>

/**
 * main - Prints a specific phrase
 *
 * Return: Always (0)
 */

int main(void)
{
	char phrase[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", phrase);
	return (0);
}
